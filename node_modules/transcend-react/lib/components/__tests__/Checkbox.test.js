'use strict';

var _react = require('react');

var _react2 = _interopRequireDefault(_react);

var _enzyme = require('enzyme');

var _reactTestRenderer = require('react-test-renderer');

var _reactTestRenderer2 = _interopRequireDefault(_reactTestRenderer);

var _Checkbox = require('../Checkbox');

var _Checkbox2 = _interopRequireDefault(_Checkbox);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

describe('Checkbox.js', function () {
  it('renders', function () {
    var component = _reactTestRenderer2.default.create(_react2.default.createElement(_Checkbox2.default, null));
    var json = component.toJSON();
    expect(json).toMatchSnapshot();
  });

  it('Controlled checkbox changes value after click', function () {
    var checked = false;

    var onChange = function onChange() {
      checked = true;
    };

    var checkbox = (0, _enzyme.mount)(_react2.default.createElement(_Checkbox2.default, { checked: checked, id: '1', onChange: onChange }));

    expect(checked).toEqual(false);
    checkbox.find('input').simulate('change');
    expect(checked).toEqual(true);
  });

  it('Uncontrolled checkbox changes value after click', function () {
    var checkbox = (0, _enzyme.mount)(_react2.default.createElement(_Checkbox2.default, { defaultChecked: true, id: '1' }));

    expect(checkbox.find('input').html()).toEqual('<input type="checkbox" id="1">');
    checkbox.find('input').simulate('change');
    expect(checkbox.find('input').html()).toEqual('<input type="checkbox" id="1" checked="">');
  });
});