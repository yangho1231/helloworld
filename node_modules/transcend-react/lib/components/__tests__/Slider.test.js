'use strict';

var _react = require('react');

var _react2 = _interopRequireDefault(_react);

var _enzyme = require('enzyme');

var _reactTestRenderer = require('react-test-renderer');

var _reactTestRenderer2 = _interopRequireDefault(_reactTestRenderer);

var _Slider = require('../Slider');

var _Slider2 = _interopRequireDefault(_Slider);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

describe('Slider.js', function () {
  var wrapper = void 0;

  it('should match base render', function () {
    var component = _reactTestRenderer2.default.create(_react2.default.createElement(_Slider2.default, {
      onChange: jest.fn(),
      id: 'whitelabel',
      value: 'whitelabel'
    }));
    var json = component.toJSON();
    expect(json).toMatchSnapshot();
  });

  it('should call onChange', function () {
    wrapper = (0, _enzyme.mount)(_react2.default.createElement(_Slider2.default, {
      onChange: jest.fn(),
      id: 'whitelabel',
      value: 'whitelabel'
    }));
    wrapper.find('input').simulate('change');
    expect(wrapper.prop('onChange')).toHaveBeenCalledTimes(1);
  });
});