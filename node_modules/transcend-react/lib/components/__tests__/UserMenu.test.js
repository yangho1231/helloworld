'use strict';

var _react = require('react');

var _react2 = _interopRequireDefault(_react);

var _reactTestRenderer = require('react-test-renderer');

var _reactTestRenderer2 = _interopRequireDefault(_reactTestRenderer);

var _UserMenu = require('../UserMenu');

var _UserMenu2 = _interopRequireDefault(_UserMenu);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

jest.mock('react-dom');
describe('UserMenu.js', function () {
  it('should match base render', function () {
    var tree = _reactTestRenderer2.default.create(_react2.default.createElement(_UserMenu2.default, {
      user: {
        first_name: 'David',
        last_name: 'Blaine'
      }
    }));
    var json = tree.toJSON();
    expect(json).toMatchSnapshot();
  });

  it('should match showSupport', function () {
    var tree = _reactTestRenderer2.default.create(_react2.default.createElement(_UserMenu2.default, {
      user: {
        first_name: 'David',
        last_name: 'Blaine'
      },
      showSupport: true
    }));
    var json = tree.toJSON();
    expect(json).toMatchSnapshot();
  });
});