'use strict';

var _react = require('react');

var _react2 = _interopRequireDefault(_react);

var _reactTestRenderer = require('react-test-renderer');

var _reactTestRenderer2 = _interopRequireDefault(_reactTestRenderer);

var _DropdownChecklistContent = require('../DropdownChecklistContent');

var _DropdownChecklistContent2 = _interopRequireDefault(_DropdownChecklistContent);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

jest.mock('react-dom');
describe('DropdownChecklistContent.js', function () {
  it('should match base render', function () {
    var testCallback = jest.fn();
    var tree = _reactTestRenderer2.default.create(_react2.default.createElement(_DropdownChecklistContent2.default, {
      textKey: 'textKey',
      valueKey: 'valueKey',
      imageKey: 'imageKey',
      items: [{
        textKey: 'test render',
        valueKey: 'test123',
        imageKey: 'https://testimage.com'
      }],
      onChange: testCallback
    }));
    expect(tree.toJSON()).toMatchSnapshot();
  });

  it('should render item as selected when included in selectedItems', function () {
    var testCallback = jest.fn();
    var tree = _reactTestRenderer2.default.create(_react2.default.createElement(_DropdownChecklistContent2.default, {
      textKey: 'textKey',
      valueKey: 'valueKey',
      imageKey: 'imageKey',
      items: [{
        textKey: 'test render',
        valueKey: 'test123',
        imageKey: 'https://testimage.com'
      }],
      selectedItems: [{
        textKey: 'test render',
        valueKey: 'test123',
        imageKey: 'https://testimage.com'
      }],
      onChange: testCallback
    }));
    expect(tree.toJSON()).toMatchSnapshot();
  });
});